# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: Patty Yau
email: patty.yau@gmail.com
description: Professional portfolio for Patty Yau
url: "https://pattyyau.github.io" # the base hostname & protocol for your site, e.g. http://example.com
baseurl: "" # the subpath of your site, e.g. /blog

# Social
twitter_username: woshipy
github_username:  pattyyau

# Build settings
permalink: /blog/:title/
excerpt_separator: <!--more-->
timezone: America/Los_Angeles
theme: null
markdown: kramdown
plugins:
# - jekyll-feed
# - jekyll-paginate

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
exclude:
  - .sass-cache/
  - .jekyll-cache/
  - gemfiles/
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/
  - Gruntfile
  - package.json
  - package-lock.json